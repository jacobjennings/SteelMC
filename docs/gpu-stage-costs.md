# SteelMC GPU stage cost measurements

This note measures where overworld terrain generation time goes on branch `experiment/gpu-worldgen`.

## Status

Measurement is in progress. This early revision records the required scope before build and benchmark results are available.

The worktree was verified with these commands:

```text
pwd
git status --short --branch
git remote -v
```

The worktree was `/home/jakej/gh/cubiomes-finds-worktrees/steelmc-gpu-moonshot`. The branch was `experiment/gpu-worldgen`, tracking `origin/experiment/gpu-worldgen`.

## Measured numbers

No timing numbers have been collected yet.

## Inferred numbers

No timing numbers have been inferred. Percentages and the Amdahl ceiling will only be calculated from measured Criterion medians.

## Load observations

No timing run has started yet.

